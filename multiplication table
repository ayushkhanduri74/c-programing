#include <stdio.h>

int main(void) {
  int n,i;
  printf("Enter The Number which Tables you want\n");
  scanf("%d",&n);
  for(i=1;i<=10;i++)
    {
      printf("%dx%d=%d\n",n,i,n*i);
    }
  return 0;
}
